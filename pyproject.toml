[build-system]
requires = [
    "cython >= 0.29",
    "cython-setuptools",
    "pysam >= 0.15",
    "setuptools >= 45",
    "setuptools_scm",
    "tox",
    "wheel"
]
build-backend = "setuptools.build_meta"

